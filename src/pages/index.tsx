import Header from '../components/Header';

const Home: React.FC = () => {
    return (
        <div>
            <Header />
            {/* Autres sections de la page d'accueil */}
        </div>
    );
};

export default Home;
